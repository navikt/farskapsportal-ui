import { BodyShort } from '@navikt/ds-react';
import { FormattedMessage } from 'react-intl';

function FarUtenErklaering() {
    return (
        <BodyShort>
            <FormattedMessage id="oversikt.infoPanel.far.utenErklaering" />
        </BodyShort>
    );
}

export default FarUtenErklaering;
