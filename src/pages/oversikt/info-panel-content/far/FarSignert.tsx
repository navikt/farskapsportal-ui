import { BodyShort } from '@navikt/ds-react';
import { FormattedMessage } from 'react-intl';

function FarSignert() {
    return (
        <BodyShort>
            <FormattedMessage id="oversikt.infoPanel.far.signert" />
        </BodyShort>
    );
}

export default FarSignert;
